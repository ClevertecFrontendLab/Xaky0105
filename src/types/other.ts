export const enum TypeSortMainPage {
  list = 'list',
  tile = 'tile',
}

export enum RoutePath {
  main = '/',
  booksAll = '/books/all',
  booksCategory = '/books/:category',
  terms = '/terms',
  contract = '/contract',
  bookPage = '/books/:category/:bookId',
  profile = '/profile',
}
